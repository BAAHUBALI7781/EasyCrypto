const Header = ()=>{
    const style={
        textAlign:"center",
        marginTop:"1rem"
    }
    return (
        <h3 style={style}>Easy-Crypto is a crypto comparator, where your can compare latest 100 cryptocurrencies</h3>
    );
}

export default Header;